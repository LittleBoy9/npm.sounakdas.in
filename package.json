{
  "name": "npm-sounakdas-in",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build && vite build --ssr src/entry-server.tsx --outDir .ssr-build && node scripts/prerender.js",
    "preview": "vite preview",
    "sync": "node scripts/sync-packages.js",
    "deploy": "node scripts/sync-packages.js && bun run build",
    "prerender": "node scripts/prerender.js"
  },
  "dependencies": {
    "framer-motion": "^12.7.3",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-router-dom": "^7.15.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.2",
    "@types/react-dom": "^19.1.2",
    "typescript": "~5.8.3",
    "vite": "^6.3.2",
    "@vitejs/plugin-react": "^4.4.1"
  }
}
